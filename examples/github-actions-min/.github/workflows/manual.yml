name: Manual

on:
  workflow_dispatch:
    inputs:
      message:
        description: "Message to print"
        required: true
        default: "hello"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Print input
        run: echo "message=${{ inputs.message }}"

